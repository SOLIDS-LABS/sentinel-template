# Lab 3 — `libipc`: the operations centre

**Due: November 20, 2026**

Worker threads inside the centre that process reports in parallel and keep the track picture.

The deliverable for this lab is released before the lab. This library uses `libproc` and
`libsyscall`.
